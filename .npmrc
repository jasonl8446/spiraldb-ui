# The agent sandbox only grants write access to this workspace, so npm's default
# cache (~/.npm) is unwritable here. Keep the cache, logs and prebuild/header
# downloads workspace-local instead of requiring a writable $HOME.
cache=.npm-cache