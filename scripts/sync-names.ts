/**
 * `npm run sync` entry point.
 *
 * Task 1.6 replaces this stub with the real friendly-name sync pipeline
 * (revision resolver → WAD unpack → template/.lang parsers → transactional
 * replace) and reuses the same service module as `POST /api/sync`
 * (Docs/plan-phase-1-foundation.md §1.4g).
 */
console.log('sync pipeline arrives in task 1.6');
process.exit(1);
