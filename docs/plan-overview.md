# SpiralDB UI — Implementation Plan: Overview

**Status:** approved — 2026-09-26, by owner decision in the execution-model interview (D29); no implementation has started
**Date:** 2026-09-26 (rev 3 — execution model re-fixed for the unattended degraded-ralph run: D16 superseded by D29, D24 amended, CI skeleton pulled forward into the preflight PR)
**Sources:** the five spec documents in `docs/`, `AGENTS.md` (Implementation Phases), a same-day environment probe of this machine, and a read-only survey of the live SpiralDB fork / Imlight loader (results below).

## Execution model (re-fixed by owner decision, 2026-09-26 — D29; supersedes the 2026-09-25 autopilot model)

- **Harness:** `ralph` in its goal-driven form ("degraded ralph" — this deployment exposes no `ralph` tool, so the loop rides native dsh goal continuation: `create_goal` + one story per round; the skill announces the degradation at launch). Single launch from a fresh session; unattended until verifier sign-off; circuit breaker `max_goal_rounds = 120`. Execution ledger: the structured PRD `.omd/prd/spiraldb-ui.json` (gitignored), generated from the five phase docs — **the phase docs + this overview remain authoritative**; divergence resolves toward them and is recorded via `prd_amend` (fail-closed amendment ledger). Completion is tool-enforced: `prd_check` rejects any `passes:true` claim without raw command evidence.
- **Branches/PRs + self-merge:** one branch per phase (`phase-1-foundation`, …), cut from `main`; a PR per phase via GitHub MCP (`jasonl8446/spiraldb-ui`), description linking the phase's `docs/evidence/phase-{n}/` artifacts. **The agent merges its own PR via GitHub MCP once (a) CI is green and (b) the phase-boundary gate story has `passes:true` with evidence**; the next phase branches from the updated `main`. Merges are logged in `.omd/prd/progress.txt`. (The former "no merge wait / change-request folded forward" chaining existed because autopilot could not merge for itself; D29 replaces it.)
- **Regression gate:** at every phase boundary a PRD gate story (`gate-1`…`gate-4`) re-runs the full acceptance suite of all previous phases before any later-phase story may pass; `final-verify` is the last full sweep.
- **Final gate (ultragoal 3-part; PRD stories `final-deslop` / `final-review` / `final-verify`):** ai-slop-cleaner pass over the cumulative diff → independent `omd-agent-code-reviewer` review (fresh subagent context; findings become fix work, never self-approved where authored) → `verify` pass re-running every phase's Verification Steps. Afterwards the loop-level `omd-agent-verifier` sign-off sets `architectVerified` on every story.
- **Ambiguity auto-resolution rule (standing instruction):** any ambiguity not covered by specs/plan/this decision list is resolved by following the detailed specs (`spec-domain-reference.md` wins on domain shape, `spec-data-model.md` on storage/naming, `spec-api.md` on endpoints), recording the decision as a new D-item in this file, and continuing. Never silently improvise; never stall waiting for a human. PRD-side twin: an empirically false criterion is amended through `prd_amend` (evidence + reason mandatory), never deleted or weakened silently.
- **UI evidence protocol:** UI acceptance criteria are proven with Playwright MCP — DOM assertions plus screenshots committed as evidence artifacts under `docs/evidence/phase-{n}/`. "Manual browser walkthrough" is not valid unattended evidence.
- **No-human contract:** between launch and verifier sign-off the loop performs zero `ask_user_question` calls and requests zero approvals; a fundamental blocker stops the run with a report instead (ralph stop condition). The human surface is exactly: preflight (✅ done — this revision's PR was the write-path smoke), the launch paste, suspend inhibition + quota check (prerequisites 4–5 below), +1 relaunch only after an abnormal exit, and post-hoc review of merged `main`.

### Owner prerequisites (the only human actions around the run)

1. ✅ **DONE (2026-09-25): .NET 9 SDK installed system-wide** via NixOS config — `dotnet --version` → **9.0.318** (`/run/current-system/sw/bin/dotnet`); sandbox-safe smoke build of `Imview.PacketReader` succeeded (0 errors, Imview tree untouched — see D18 for the proven flags).
2. ✅ **DONE (2026-09-26): plan approved** by the owner's execution-model interview decisions (D29); the status line above is flipped.
3. ✅ **DONE (2026-09-26): preflight write-path smoke** — this revision landed as the PR `preflight-ralph-execution-model`: branch push → PR opened → Actions green → GitHub-MCP merge → branch deleted, proving every unattended operation class (push, PR, CI poll, self-merge) before launch.
4. **Inhibit suspend for the run duration** (this machine is s2idle-capable; `sleep/suspend/hibernate` targets are linked): keep `systemd-inhibit --what=sleep --why="spiraldb-ui ralph run" --mode=block sleep infinity` alive in a background shell. An idle suspend parks the goal loop, and resuming costs a human keystroke.
5. **Confirm quota headroom immediately before launch** — `token_plan_quota` errored on read during preflight (2026-09-26, "not lossless JSON"); retry it and confirm ~120 rounds × executor subagents fit the budget.

Real packet captures are **no longer required** — decision D28 manufactures them from real fork quests (owner decision 2026-09-25). If a real capture ever surfaces (e.g. from the Oct 2025 "jay" imports), it is optional post-hoc validation, replayable against the Phase 2 acceptance suite.

This document is the entry point for the phased build plan. One document per phase:

| Phase | Document | Depends on |
|---|---|---|
| 1 — Foundation | [plan-phase-1-foundation.md](./plan-phase-1-foundation.md) | — |
| 2 — Quest Extraction | [plan-phase-2-quest-extraction.md](./plan-phase-2-quest-extraction.md) | Phase 1 + .NET SDK (Q2) |
| 3 — Quest Editing | [plan-phase-3-quest-editing.md](./plan-phase-3-quest-editing.md) | Phase 2 |
| 4 — Other Object Editors | [plan-phase-4-object-editors.md](./plan-phase-4-object-editors.md) | Phase 1; reuses Phase 3's RequirementTreeEditor |
| 5 — Dashboard & Polish | [plan-phase-5-dashboard-polish.md](./plan-phase-5-dashboard-polish.md) | Phases 1–4 |

```
[1 Foundation] ──► [2 Quest Extraction] ──► [3 Quest Editing] ──┐
       │                                                          ├──► [5 Dashboard & Polish]
       └────────────────► [4 Object Editors] ─────────────────────┘
                          (reuses 3's RequirementTreeEditor)
```

Phases 3 and 4 may overlap once the RequirementTreeEditor (task 3.6) exists.

## How to use this plan

- Each phase document contains: Requirements Summary, Tasks (numbered `P.T`), Acceptance Criteria (testable checkboxes), Risks & Mitigations, and Verification Steps.
- The ralph PRD (`.omd/prd/spiraldb-ui.json`, gitignored) is the execution ledger generated from these docs: story ids `p{phase}-{nn}` (tasks), `gate-{n}` (phase-boundary regressions), `final-*` (ultragoal gate). The phase docs stay the single home of the acceptance criteria — regenerate the PRD if they change.
- Work one phase per branch/PR. A phase is **Done** only when every acceptance criterion passes with recorded evidence (command + observed output).
- Task IDs are stable references — cite them in commits (`plan: task 2.4 save pipeline`) and PR descriptions.
- When implementation reveals a spec gap, record the resolution in **Cross-Cutting Decisions** below (or the phase doc's Decisions section) rather than silently diverging.

## Verified environment baseline (probed 2026-09-25)

Facts confirmed on this machine; plans and acceptance criteria rely on them.

| Fact | Value | Consequence |
|---|---|---|
| Node / npm | v24.21.0 / 11.19.0 | ✅ Meets Node 18+ requirement |
| git | 2.54.0 | ✅ |
| .NET SDK | ✅ **installed system-wide 2026-09-25**: dotnet 9.0.318 at `/run/current-system/sw/bin/dotnet`; `Imview.PacketReader` smoke build passes with D18 artifacts-layout flags (0 errors, 64s, Imview tree untouched) | Phase 2 gate **cleared**; D18 build recipe proven |
| `imcodec` CLI | ✅ prebuilt binary runs: `/home/jason/Documents/git-projects/Imview/submodule/Imcodec/src/Imcodec.Cli/bin/Debug/net9.0/linux-x64/imcodec` (commands: `wad`, `op`, `bcd`) | Phase 1 sync does **not** need the .NET SDK → decision D3 |
| Root.wad | ✅ `/home/jason/Documents/git-projects/Aurorium/data/V_r806919.Wizard_1_610/Data/GameData/Root.wad` (295,405,277 bytes) | Matches the path pattern in [spec-domain-reference.md](./spec-domain-reference.md) L641–644; revision matches the spec's example |
| Imview.PacketReader | ✅ `/home/jason/Documents/git-projects/Imview/src/Imview.PacketReader/`, `net9.0`; `QuestBuilder.cs` L33: `public static async Task<List<QuestTemplate>> BuildQuestsFromPacketCaptureAsync(string packetCapturePath)` | Exactly the API assumed by [spec-domain-reference.md](./spec-domain-reference.md) L585 |
| SpiralDB repo | ✅ `/home/jason/Documents/git-projects/spiraldb/`, branch `main`, clean; **322** QuestTemplates, **317** DropTables; **no `NpcDropTable/` directory** | Baseline counts for acceptance criteria; import scan must tolerate missing directories ([spec-domain-reference.md](./spec-domain-reference.md) L21) |
| Sample packet captures | ❌ none anywhere on this machine (full-disk search 2026-09-25); Imview has **no live-capture feature** — "Get Quests From Packet Capture" is a file picker feeding `QuestBuilder` (`QuestPacketReaderService.cs` L59) | Not a blocker: D28 fixture generator synthesizes captures from real fork quests |
| SpiralDB remote | ✅ the local clone **is the owner's fork**: `origin=git@github.com:jasonl8446/spiraldb.git`, `upstream=Revive101/spiraldb` | Default `spiraldb_path` = this fork (D17) |
| Legacy filenames | ❌ do **not** follow the new-file convention: `droptables_ds-acad1-c01-001.json` (plural prefix), `NPCInventories_1025-A.json`, `WizardZoneDatas_10017-A.json`, `NpcTreasureCards_2019-A.json`; QuestTemplates **do** match (`questtemplates_{name}.json`) | Detail lookup + updates must use a content-keyed index and write back to the original path (D19) |
| QuestMetadatas | 324 files, **UUID-named** (e.g. `069f430e-….json`), paired to quests by the `Name`/`QuestTemplateId` field **inside** the file | Metadata updates must locate by content, not filename (D20) |
| GlobalRegistry | exactly one legacy file `GlobalRegistryModels_1-A.json` (23 keys); Imlight merges `Directory.EnumerateFiles` **unsorted, later wins** (`Imlight/src/Imlight.CoreLib/WizardData/SpiralDB.cs` L341–347) | Consolidate & replace on save (D22) |
| Full corpus counts | QuestTemplates 322, DropTables 317, NpcInventory 215, NpcSpellInventory 77, CreatureSpellbook 134, TreasureCardInventory 1, ZoneTransfer 1207, NpcDropTable **0 (dir absent)** | Acceptance baselines for Phases 1/4 |
| ZoneTransfer schema drift | real files contain an `Events` field absent from the spec schema | Merge-not-replace (D5) preserves it; editor shows it in raw-fields disclosure (task 4.8) |
| This repo | Docs only (`AGENTS.md`, `README.md`, `docs/` × 5); HEAD `21596d5`; remote `jasonl8446/spiraldb-ui`, branch `main` | Greenfield; PR-per-phase workflow available |

## Cross-cutting decisions

Decisions the specs leave open, resolved here. Amend this list when reality disagrees.

- **D1 — Repository layout.** Single npm package (no workspaces) with:
  ```
  server/            # Express + TypeScript (tsx in dev, tsc for build)
    src/{index.ts, db.ts, routes/, services/}
  client/            # Vite + React app (vite root = client/)
    src/{components/, features/, hooks/, lib/}
  shared/            # Types + Zod schemas imported by both server and client
  scripts/           # sync-names.ts (npm run sync)
  tools/PacketReaderCli/   # .NET 9 wrapper (Phase 2)
  data/              # SQLite db (gitignored, per spec-architecture L146–154)
  ```
  Rationale: local single-user tool; one lockfile and one `npm run dev` (concurrently) beat workspace ceremony.
- **D2 — Add `multer`.** `POST /api/extract/quests` is multipart ([spec-api.md](./spec-api.md) L210–214) but no upload library appears in the spec's package list ([spec-architecture.md](./spec-architecture.md) L181–198). Use multer with disk storage, 512 MB cap.
- **D3 — Default `imcodec_path`.** The verified prebuilt binary path from the baseline table (spec allows "known Imview submodule path", [spec-data-model.md](./spec-data-model.md) L166). Override stays possible via Settings.
- **D4 — Status type-name mapping.** API routes use plural types (`quests`, `drop_tables`, … [spec-api.md](./spec-api.md) L56) while `entry_status.object_type` stores singular values (`quest`, `drop_table`, … [spec-data-model.md](./spec-data-model.md) L32–37). One mapping constant in `server/src/routes/status.ts`; unit-tested both directions.
- **D5 — Merge-not-replace serialization (fidelity rule).** Editors never rebuild a document from a partial schema. Load: `JSON5.parse` the original; keep it. Edits mutate/merge into the parsed original; unknown fields survive untouched. Save: `JSON.stringify(doc, null, 2)` ([spec-data-model.md](./spec-data-model.md) L238–253). Enforced by the corpus round-trip tests (tasks 3.2, 4 acceptance).
- **D6 — No DELETE endpoints in v1.** [spec-api.md](./spec-api.md) L174–204 defines only GET/GET-by-key/POST. Deletion remains a manual file+git operation. Revisit via Q3.
- **D7 — Add `@tanstack/react-query`.** Not in the spec stack table, but 15k-row name lists need caching + invalidation after sync/save; it is the least-code option consistent with the TanStack Table choice.
- **D8 — FriendlyNameDropdown implementation.** shadcn/ui Combobox (cmdk) over the cached `/api/names/:type` list, client-side filtering; raw ID in a hidden field (AGENTS.md rule 5). NPCs render `"Name (TemplateID)"` uniformly ([spec-domain-reference.md](./spec-domain-reference.md) L696–698); zones render the humanized form (L700–702). If >5k rows proves sluggish, add virtualization — measure first.
- **D9 — Extraction cancel.** Server tracks the extraction child process; client uses `AbortController`; on request abort the server kills the child. Cancel button per [spec-ui-design.md](./spec-ui-design.md) L216.
- **D10 — Test stack.** Vitest + Supertest (server); no e2e framework in v1 — each phase has a manual verification checklist instead. The corpus round-trip test is the primary safety net for Phases 3–4.
- **D11 — Commit conventions.** Agent-authored commits *in this repo* carry the DeepSeek Harness watermark trailer (AGENTS.md → `commit-watermark` skill). The app's *runtime* auto-commits to the SpiralDB repo use `spiraldb: {action} {object_type} {object_key}` authored by `settings.user_name` ([spec-data-model.md](./spec-data-model.md) L216–232) — no watermark there.
- **D12 — List endpoints read on demand.** `GET /api/quests` scans the directory and JSON5-parses files per request (322 local files is trivial); join with `entry_status` for status. Add caching only if measured slow. Quest list search/filter is client-side over the loaded list.
- **D13 — One commit per object save.** "Save All" of N extracted quests produces N sequential commits (message format is per-object, [spec-data-model.md](./spec-data-model.md) L216–223).
- **D14 — Dirty-repo guard.** Before any save, check `git status --porcelain` in the SpiralDB repo. If dirty, fail with an actionable error toast ("SpiralDB repo has uncommitted changes — resolve them first"). Never auto-stash user work.
- **D15 — Status transitions.** Any transition is allowed (including backwards, e.g. verified→reviewed after a regression); every change is logged to `status_history` with old/new/notes/changed_by. UI primarily surfaces forward actions ("Mark Reviewed", "Mark Verified"). Spec requires history preservation but forbids no direction ([spec-data-model.md](./spec-data-model.md) L9–19).
- **D16 — Execution harness & chaining.** Autopilot; phase branches + PRs; phase N+1 starts from phase N's branch HEAD without waiting for merge; full-regression gate at every phase boundary; ambiguity auto-resolution rule (see Execution model above). Owner-approved 2026-09-25. **SUPERSEDED by D29 (2026-09-26)** — vehicle changed to degraded ralph; no-merge-wait chaining replaced by agent self-merge at green gates. The regression gate and the ambiguity rule carry over unchanged.
- **D17 — SpiralDB target = owner's fork, configurable.** Default `settings.spiraldb_path` = `/home/jason/Documents/git-projects/spiraldb` (verified: `origin=jasonl8446/spiraldb`). Fully configurable via Settings/API. **Automated tests never touch it**: dev/test configuration points at `data/test-spiraldb/`, a disposable in-workspace `git clone` of the fork (gitignored, created by task 2.0, refreshed by re-clone). The real fork is written only by the app in owner-driven production use.
- **D18 — .NET builds are sandbox-safe (flags proven 2026-09-25).** Owner installed the SDK system-wide (prerequisite 1 ✅: dotnet 9.0.318). All `dotnet build`/`restore` invocations use the **artifacts output layout**: `-p:UseArtifactsOutput=true -p:ArtifactsPath=$PWD/tools/.artifacts` plus env `NUGET_PACKAGES=$PWD/tools/.nuget` — per-project obj/bin isolation under one in-workspace root, zero writes into the Imview tree or `$HOME` (both verified: smoke build of `Imview.PacketReader` = 0 errors in 64s, Imview tree untouched, `~/.nuget` untouched, 287 MB of packages landed in `tools/.nuget`). ⚠️ **Never** use `-p:BaseIntermediateOutputPath`/`-p:BaseOutputPath` for these builds — they are global properties, so every referenced Imcodec project shares one obj directory and the build dies with CS0579 duplicate-attribute errors (empirically proven 2026-09-25). `tools/.artifacts/`, `tools/bin/`, `tools/.nuget/` are gitignored; build scripts symlink final executables into `tools/bin/` so the spec's `tools/bin/imview-packet-reader` path holds ([spec-domain-reference.md](./spec-domain-reference.md) L595–602). Note: `Imview.PacketReader` is a class library — the runnable exes are our wrapper (2.1) and fixture generator (2.1a).
- **D19 — Content-keyed index, original-path updates.** Because legacy filenames don't follow the convention (baseline table), `GET /:key` and update-saves resolve files through an in-memory index built by scanning each type's directory and JSON5-parsing the key field (rebuilt on save/startup; ~2,300 files total — trivial locally). **Updates always write back to the file's original path**; the `{prefix}_{key}.json` convention applies only to newly created entries.
- **D20 — Quest metadata pairs by content.** Existing metadata files are UUID-named; pairing is via the `Name` field ([spec-data-model.md](./spec-data-model.md) metadata shape). On quest save: scan `QuestMetadatas/` for a file whose `Name` matches → update it in place (refresh `ModifiedAt/ModifiedBy`); only when none exists create `questmetadata_{name}.json`.
- **D21 — Corpus-derived quests & zones tables.** Sync builds `quests` from the SpiralDB `QuestTemplates/` corpus (`m_questName`, `m_questLevel`, `m_mainline`, title via `string_table` lookup of `m_questTitle`) and `zones` from all distinct `ZoneName` + `m_destinationZone` values in the `ZoneTransfer/` corpus (1,207 files). WAD-derived sources are used only if spike 1.4a finds an authoritative list in the unpack. Owner-approved.
- **D22 — GlobalRegistry consolidate & replace.** Save writes the merged dictionary to `globalregistry.json` **and `git rm`s every other `GlobalRegistry/*.json` in the same commit** (`spiraldb: update global_registry globalregistry`). Rationale: Imlight's unsorted later-wins merge makes multiple files order-dependent; one file is deterministic; git history is the undo; the PR diff shows the consolidation before merge. Supersedes the Q5 warning-only approach.
- **D23 — Two-tier UI testing (Playwright validated end-to-end 2026-09-25).** **Tier 1 — in-repo Playwright suite (durable, CI-runnable):** `@playwright/test` (pinned version) specs in `tests/ui/`, headless via `npm run test:ui`; browsers isolated workspace-local (`PLAYWRIGHT_BROWSERS_PATH=$PWD/tools/.playwright-browsers`, gitignored — same isolation philosophy as D18; do not rely on the Nix store browsers, whose revision is coupled to the system playwright package). Every phase ships specs for its flows: P1 shell navigation, P2 extraction (driven by D28-generated captures), P3 editor interactions, P4 list/detail per type, P5 a11y (`@axe-core/playwright`) + responsive viewports (375/768/1440). **Tier 2 — agent-side playwright-mcp evidence for acceptance runs:** validated workflow — serve the app over HTTP (`file://` is **blocked** by the MCP server), assert via `browser_snapshot`/`browser_evaluate` (returns JSON), screenshot with an **absolute path under `/home/jason/.cache/playwright-mcp/`** (the only writable allowed root; relative filenames resolve against the server cwd and fail — pinned via `--output-dir` in `~/.dsh/cordis.patch.yml` after the root-owned-cwd EACCES was diagnosed), then `cp` into `docs/evidence/phase-{n}/` and commit. Smoke proof: `docs/evidence/playwright-mcp-smoke-validated.png` (912×930) and `playwright-mcp-smoke-mobile-375.png` (exact 375×667 viewport emulation). "Manual browser walkthrough" is not valid unattended evidence.
- **D24 — Minimal CI.** `.github/workflows/ci.yml`: on PR → `npm ci && npm test && npm run build && npm run test:ui` (headless chromium; workflow runs `npx playwright install chromium` first). Objective signal for the self-merge gate (D29). **Amended 2026-09-26:** the workflow file was pulled forward into the preflight PR with its npm/playwright steps gated on `package.json` existing, so Actions is proven live before Phase 1; task 1.9 makes the full pipeline run for real on the Phase 1 PR (its acceptance criterion is unchanged: verified by that PR's own check run).
- **D25 — Approved dependency additions.** `multer` (D2), `@tanstack/react-query` (D7), `@dnd-kit/sortable` (goal reordering, task 3.4), `dagre` + `@types/dagre` (flowchart auto-layout, task 3.5), `react-json-view-lite` (JSON side panel, task 2.7), ESLint + Prettier (scaffold, task 1.1). Owner-approved.
- **D26 — New files use the spec prefix even where legacy differs.** New DropTables are `droptable_{Name}.json` (singular, per [spec-data-model.md](./spec-data-model.md) L173–187) although legacy files use `droptables_`. Imlight loads by directory; the mixed prefixes are cosmetic. Owner-approved.
- **D27 — Phase 5 API extensions.** Two endpoints beyond [spec-api.md](./spec-api.md): `GET /api/activity?limit=10` (status_history × entry_status join for the dashboard feed) and `GET /api/search?q=&limit=20` (cross-type key + friendly-name search for the ⌘K palette). Task 5.7 appends both to `spec-api.md` so the spec stays authoritative.
- **D28 — Synthetic capture fixture generator (no real captures needed).** A small .NET tool `tools/FixtureGen/` (same pattern as the CLI wrapper, sandbox-safe builds per D18) manufactures packet-capture files from **real fork quests**: deserialize a corpus `questtemplates_*.json` via Imcodec ObjectProperty (the corpus `$type` format is Imcodec's own), serialize its goal/dialog structures into the hex blobs QuestBuilder consumes (`GoalCompilation` offset 1, `ActorDialog` offset 16, `ClientTagList` offset 1, `MadlibBlock` offset 1 — verified in `QuestBuilder.cs` L164–509), and wrap them in `MSG_QUESTOFFER`/`MSG_SENDQUEST`/`MSG_SENDGOAL`/`MSG_ACTORDIALOG` envelopes (`{"data":{"name":…,"fields":{…}}}` — verified in `PacketReaderService.cs` L100–130). Phase 2 acceptance becomes a **closed-loop round-trip**: quest → capture → extract → quest′ ≈ quest, reproducible in every test run. Fallback ladder if blob layouts resist: minimal valid envelope with thin blobs (proves upload→CLI→review→save→commit plumbing), with deep reconstruction flagged in the PR. Owner-approved 2026-09-25.
- **D29 — Execution vehicle: degraded ralph + ultragoal final gate; agent self-merge (owner decisions, 2026-09-26 execution-model interview).** Supersedes D16. One human launch from a fresh session; the loop is ralph's documented degradation path (goal-driven, one story per round — no `ralph` tool exists in this deployment) over a single structured PRD `.omd/prd/spiraldb-ui.json` (62 stories: 55 task stories from the 53 numbered tasks + 4 phase-boundary gate stories + 3 ultragoal final-gate stories), `max_goal_rounds = 120` (sized ≈1.5× the happy-path story count; re-derive as stories × 1.5 + 10 if the PRD diverges). Evidence is tool-enforced (`prd_check` rejects evidence-less claims; `prd_amend` keeps a fail-closed ledger). Implementation delegates per story to fresh `omd-agent-executor` subagents, keeping the orchestrator context thin. Per-phase branch + PR remains the review artifact; **the agent merges each PR via GitHub MCP when CI is green AND the gate story passed with evidence**; the next phase branches from updated `main`; merges are logged in `progress.txt`. Rejected alternatives: **autopilot** (Phase-0 `ask_user_question` seams fail closed unattended; QA + dual validation end-loaded instead of per-phase; phase-granular resume; its ralplan/deep-interview linkage clauses don't recognize this repo's plan artifacts); **per-phase goal/ralph chaining** (ralph refuses to start under an active goal continuation; `create_goal` is not authorized during continuation rounds — a chain stalls after phase 1); **five separate runs** (five human launches). Known residual human touch: one relaunch after an abnormal exit (neither ralph nor goal auto-resumes; stale state is reported, never continued). **Branch protection on `main` (added post-preflight, 2026-09-26):** require a pull request before merging (required approvals = **0**, deliberately — GitHub forbids approving your own PR with the same account, so any review requirement would put a human back in the loop) + required status check **`ci`** + `enforce_admins` — the CI-green half of the self-merge gate is platform-enforced rather than agent discipline; the gate-story-evidence half stays a PRD contract.

- **D30 — Dependency floors and cache relocation for Node 24 (recorded during task 1.1, 2026-09-26).** Two deliberate divergences from the package list in [spec-architecture.md](./spec-architecture.md) L181–198, both required for a green install on the verified baseline (Node v24.21.0 = `NODE_MODULE_VERSION` 137): **(a)** `better-sqlite3` floor raised from `^9.0.0` to **`^12.11.1`** — 9.x publishes no v137 prebuild and this NixOS machine has no `make` (only gcc/g++), so node-gyp fails with `gyp ERR! not ok`; 12.11.1 ships a `node-v137-linux-x64` prebuild (verified: `npm install` and `npm ci` both exit 0, `new Database(':memory:')` + `select 1` works). Do **not** move to v13 — it publishes no v137 prebuild. **(b)** the repo carries `.npmrc` with `cache=.npm-cache` because the agent sandbox cannot write `$HOME` (plain `npm install` dies with `EACCES … /home/jason/.npm/_cacache`); the cache (and node-gyp/prebuild headers under it) lives inside the workspace and is gitignored. Rationale: the toolchain must install and build unattended; both changes are invisible to the app's runtime behaviour.

- **D31 — Data-layer mechanics resolved during task 1.2 (2026-09-26).** Four points the specs leave open, now fixed so later tasks depend on them knowingly: **(a) migrations live at `server/migrations/NNNN_*.sql`** as plain idempotent SQL, and `build:server` appends `node scripts/copy-server-assets.mjs` to copy them into the tsc output (tsc does not emit `.sql`) — a new migration is a new file only; **(b) settings seeding is env-overridable through one pure function** (`buildSeedSettings({env, now})`): precedence is explicit env (`SPIRALDB_PATH`, `AURORIUM_PATH`, `IMCODEC_PATH`, `USER_NAME`, `GIT_BRANCH`) > `NODE_ENV=test` (which yields `<repo>/data/test-spiraldb`, the D17 disposable clone) > the spec defaults (owner fork); empty-string env values count as unset; **(c) seeding is first-run-only** — `settings` is never overwritten once populated, so a user-edited path survives restart; **(d) `PRAGMA foreign_keys = ON`** is enabled, making `status_history.entry_status_id REFERENCES entry_status(id)` real: any writer that inserts history (task 1.6 transitions, Phase 2 status upserts) must ensure the `entry_status` row exists first. Verification: `npm test` (22 tests) + a fresh `rm -f data/spiraldb-ui.db && npm run build && npm run start` boot proving 11 tables / 3 named indexes / the five seeded defaults.

- **D32 — Settings API contract resolved during task 1.3 (2026-09-26).** The spec ([spec-api.md](./spec-api.md) L291–321) gives `GET`'s shape but not `PUT`'s response or its validation matrix. Fixed as: `PUT /api/settings` returns **200 with the full updated settings map** (same shape as GET, so one round trip refreshes a client); a partial body updates only the supplied keys; an **empty object `{}` is a 200 no-op**; any non-object body is 400. Validation is **atomic** (all keys checked before any write) and ordered deterministically — unknown key → non-string/null value → path kind — returning `400 {"error": …}` with the offending key, the received value, and for unknown keys the allowed-key list in `SETTINGS_KEYS` order (`aurorium_path, imcodec_path, user_name, spiraldb_path, git_branch`). `aurorium_path`/`spiraldb_path` must be existing **directories**; `imcodec_path` an existing **file**; `user_name`/`git_branch` are free text and `user_name: ""` is valid (the "not asked yet" state that task 1.7's modal keys off). Writes are `INSERT … ON CONFLICT(key) DO UPDATE` and never re-seed (D31c). Routers that need the DB mount it **lazily** (`getDb()` on first request, not at import) — importing `@server/app` in a test must not open/seed the real `data/spiraldb-ui.db`, and later routers (names/sync/status) follow the same pattern.

- **D33 — WAD/string-table reality measured by spike 1.4a (2026-09-26); overrides the family names and name-field list in [spec-domain-reference.md](./spec-domain-reference.md) L654–671.** The spike (evidence: [docs/evidence/phase-1/spike-1.4a.md](./evidence/phase-1/spike-1.4a.md)) unpacked the real Root.wad: **~17 s, 173,088 files, 1.5 GB**, exit 0, `--deser` succeeded for every family (no fallback needed; `imcodec op file` proven anyway as the documented fallback). **(a) There is no `ActorTemplate` class** — task 1.4d must discriminate by JSON `_className`, never by filename: items = `WizItemTemplate` (76,679) + `ItemBundle`/`Reagent`/`PetSnack`/`Item` (3,157) under `ObjectData/**`; spells = `SpellTemplate` (14,856) + `TieredSpellTemplate` (2,579) under `Spells/**`; NPCs = `WizGameObjectTemplate` (15,190) + `GameObjectTemplate` (7,698); pets = `WizPetTemplate` (143); mounts = `WizMountTemplate` (2). **(b) The friendly-name field is `m_displayName`, not `m_name`** (`m_name` exists only on `SpellTemplate`); read `m_templateID`/`m_objectName`/`m_displayName`, resolve the display name, and fall back `m_displayName → resolved value → raw key → m_objectName`. **(c) Scope `.lang` parsing to `Locale/en-US/*.lang`** — exactly 5,132 files / 25.9 MB (all 8 locales are 33,932 files / 185 MB). **(d) A `.lang` record is three lines — `{key}\r\n{middle}\r\n{value}` — and the key is written in one of two lexical forms.** The middle line is usually blank but not always (8,879 records locale-wide carry a label there, e.g. `MobDescriptions` = name above description), so a fixed "key, blank, value" stride mis-parses those files; ~79 tables (10,105 rows) are keyed by **name** rather than a number (`Chat.lang`, `CharCreation.lang`, `QFChooseFriend.lang`) and must be accepted verbatim; keys are sometimes unpadded. **(e) Key resolution is FORM-MATCHED (lead-verified, superseding two earlier wrong readings of this file).** For `{Category}_{suffix}`: a hex-written suffix (contains `A–F`) looks up the **hex token** first; an all-digit suffix looks up the **decimal token** first; only if that form is absent, try the other form's numeric equivalent, then fall back to the raw key. This matters because `QuestTitle.lang` writes the same numeric index **both ways with different values** (28 differing-value collisions locale-wide, 26 of them in `QuestTitle.lang`: `126346` → "Letters of Light" but `1ED8A` → "Forged in Fire"; reproduced by `scripts/lang-collision-survey.ts`, and 0 in the friendly-name categories Items/Spells/NPCs/Mobs/ZoneLocName), so normalising both sides to a number and taking "last wins" returns wrong titles. Measured over all 322 corpus quest files: 7 carry no `m_questTitle`, and **all 315 keyed quests resolve (287 hex-form, 28 decimal-form, 0 raw-key fallbacks)** — including the spec's example, `QuestTitle_1ED8D` → **"Quest for Perfection"**. P1 AC#5 and AC#6 are both satisfied with real data; no `prd_amend` was needed. **(f) `SpellTemplate`/`TieredSpellTemplate` carry no `m_templateID` and no `m_objectName`** — their only id-like field is `m_displayName` (`Spells_00000424`), so `spells.template_id` is that string-table index and the 961 rows with an empty `m_displayName` have no id at all; separately, quests reference spells by a *different* id space (`m_spellID`, e.g. 2064457962 in `ResGiveSpell` results) that no WAD family exposes, so Phase 3's spell-result dropdown must be revisited with its own probe (recorded as a known gap, not a Phase-1 requirement). Additional spell classes exist beyond the two mapped ones (`CastleMagicSpellTemplate` 290, `CantripsSpellTemplate` 162, `GardenSpellTemplate` 143, `WhirlyBurlySpellTemplate` 94, `FishingSpellTemplate` 49) — include them when a later task needs those names. Never pass `--verbose` to `wad unpack` (upstream warns it degrades performance sharply).

- **D34 — the documentation directory is `docs/` (all lowercase), renamed from `Docs/` on 2026-09-26.** Owner request for consistency with the rest of the tree. Every reference was rewritten in the same commit: code comments under `server/src`, `tests/`, `scripts/`, `shared/`, `client/` configs, `server/migrations/0001_init.sql`, `.github/workflows/ci.yml`, `.gitignore`, `AGENTS.md`, `README.md` and all plan/spec documents. **Evidence therefore lives at `docs/evidence/phase-{n}/`** (the PRD's own strings were updated to match). New documents must be created under the lowercase path — never re-introduce `Docs/`. Verified: `git grep "Docs/"` now matches only this decision's own text (it quotes the old spelling twice) and all 213 in-repo markdown links resolve.

- **D35 — the WAD's authoritative numeric id space is `TemplateManifest_deser.json` (found 2026-09-26 during p1-06 verification); supersedes D33(f)'s claim that no WAD family exposes spell ids.** The unpacked tree carries `Root.wad/TemplateManifest_deser.json` (17 MB, `_object.m_serializedTemplates`): **137,423 unique `{m_filename, m_id}` pairs**, i.e. the game's id → source-file mapping for every template (18,173 of them under `Spells/`). Measured: of the **794 spell ids the corpus actually references** (`NpcSpellInventory.Spells[].TemplateID`/`RequiredSpellID`, `CreatureSpellbook.SpellTemplateIds`, quest `m_spellID`), **792 are in the manifest and 716 point at a `Spells/*.xml` file**; resolution is real, e.g. `1143963608` → `Spells/Stun Block.xml` → `SpellTemplate` `m_name` "Stun Block" (`m_displayName` `Spells_00000548`), `2102149986` → `Spells/Conviction.xml` → "Conviction" (`Spell_00001088`), `409737272` → `Spells/Death Shield.xml` → "Death Shield". No hashing is involved — eight hash functions tested against the spell names produced one coincidental match, so id→name MUST come from the manifest. **Consequences:** (a) `spells` must be keyed by the manifest `m_id`, not by the string-table index of `m_displayName` — the index space is shared by two categories (`Spells_*` and `Spell_*`), so the current keying collides on 1,362 ids and the INTEGER-primary-key table silently drops **13,003 of 16,474** parsed spell rows (`p1-06` reported this loudly as `dropped (PK)`); (b) **no schema change is needed** — `m_id` is a unique INTEGER that fits `spells.template_id INTEGER PRIMARY KEY` and is exactly what the corpus references, so Phase 3/4 spell dropdowns become viable; (c) the manifest is also the better id source for items/NPCs (it covers templates whose `_deser.json` has an empty `m_templateID`). Implemented by story **p1-06b** (task 1.4h); the copy of the manifest on disk is a spike artifact — `npm run sync` must read it from the unpacked tree, and tests must inject a synthetic manifest rather than the 17 MB real one. **Measured outcome of p1-06b (2026-09-26):** `spells` went 3,471 → **18,173 rows with 0 dropped / 0 collisions** (the whole `Spells/` family, incl. the five extra `*SpellTemplate` classes), items/npcs/quests/zones/drop_tables/string_table unchanged, and corpus spell-id resolution went 0/794 → **716/716 of the ids that point at a `Spells/*.xml` file (90.2% of all referenced ids)**. The residual is data, not a bug: 76 referenced ids point at `ObjectData/*.xml` (NPC/item templates) and exactly 2 are absent from the manifest entirely — `1749527` (`NpcSpellInventory/NPCSpellInventories_1121-A.json`) and `213674121` (`CreatureSpellbook/CreatureSpellbooks_622-A.json`); Phase 4 spell dropdowns should show the raw id for those.
- **D36 — Names API contract (task 1.5 / story p1-07): the spec exemplifies only `items`, so this fixes the rest.** `GET /api/names/:type` returns `{ "<type>": [rows] }` (type name verbatim) and `GET /api/names/:type/:id` returns one bare row. Row shape = the type's primary-key column plus its display columns: `items{gid,name}` (the spec's example, reproduced byte-for-byte — `items/4808` really is "Twice Stitched Boots"), `spells{template_id,name}`, `npcs{template_id,name}`, `quests{quest_name,title,level,is_mainline}`, `zones{zone_path,display_name,world}`, `drop_tables{name,description}`, `strings{key,value,category}`. **Errors are always 404 `{error}`, never 500 or 400, for: an unknown type (message lists all seven valid types), an unknown id, and an id that cannot be valid for the type** (e.g. `items/abc` → "gid must be an integer"). A missing **string key 404s**; the *client* renders the raw key on that 404 and never shows an error (plan task 1.5, spec-domain-reference L693–694). `zones` ids contain a slash (`Aquila/AQ_Z00_Hub`) so they must be percent-encoded (`%2F`) in the single-lookup URL; the unencoded two-segment form correctly does not match. Ordering is deterministic — display column, then key column — because 4,395 item names are duplicated. Two **optional** list extensions keep the UI practical without changing the bare-URL contract: `?q=<substring>` (case-insensitive over the display column; for `strings` over key **and** value; LIKE metacharacters escaped to literals) and `?limit=<n>` (positive integer); malformed values → 400. Measured on the real synced DB: items 3,804,474 B / 0.08 s, spells 909,679 B, npcs 1,143,248 B, quests 27,772 B, zones 179,928 B, drop_tables 14,901 B, strings 24,077,358 B / 0.37 s. The `temp_store = MEMORY` PRAGMA that makes the `strings` sort fast currently lives in the names router; if a later large-result endpoint (the p1-08 dashboard) sorts heavily, it belongs in `openDb()` instead — recorded, not yet moved.
- **D37 — Status / dashboard / first-startup-import contract (task 1.6 / story p1-08).** Type names follow D4 (`quests→quest`, `drop_tables→drop_table`, `npc_inventories→npc_inventory`, `npc_spell_inventories→npc_spell_inventory`, `creature_spellbooks→creature_spellbook`, `npc_drop_tables→npc_drop_table`, `treasure_card_inventories→treasure_card_inventory`, `zone_transfers→zone_transfer`, plus the route-only value `all`); one exported constant, asserted in both directions. `GET /api/status/:type` → `{entries:[{object_type,object_key,status,extracted_at,reviewed_at,verified_at,latest_notes}],summary:{total,extracted,reviewed,verified}}` with **`object_type` singular**, `latest_notes` = the newest `status_history` notes for that entry (`null` when none), entries ordered `object_key` then `object_type` (the tiebreak matters because a key is only unique within a type, so `type=all` interleaves by key and a UI must group client-side), and **`summary` counts the whole type BEFORE the `?status=` filter** (`summary.total == extracted+reviewed+verified`; verified live: `?status=reviewed` returned 2 entries with `summary.total` 322). `?status=` accepts only the three values or nothing — an empty or unknown value is 400; an unknown `:type` is 404 listing the valid types. `PATCH /api/status/:type/:key` → 200 with the **bare updated row** (same shape as an `entries[]` element); unknown type/key → 404 (`all` is not a valid `:type` for PATCH or history — it names no entry); missing or unknown `status` → 400; `changed_by` defaults to `settings.user_name`; the write sets **only the timestamp/by pair of the new status and never clears a previously set one** (a backwards verified→reviewed keeps `verified_at`/`verified_by` as the historical record while `status` and the dashboard's `verified` count move back — the dashboard counts the current `status` column, not the timestamps), and it appends exactly one `status_history` row with `old_status`/`new_status`/`notes`/`changed_by` in the same transaction. `GET /api/status/:type/:key/history` → `{history:[{old_status,new_status,notes,changed_by,changed_at}]}` oldest→newest, `[]` for an entry with no changes. `GET /api/dashboard` → `{types,overall}` with **all eight singular keys always present** (zeros included, so the UI table is stable) and `overall.percent_verified` rounded to one decimal (`272/597 → 45.6`), `0` when `total` is 0 — never NaN/null. **First-startup import** runs only when `entry_status` is empty, in the real entrypoint (`server/src/index.ts`, never as a side effect of importing `app.ts`; `SPIRALDB_UI_SKIP_IMPORT=1` is the escape hatch for tests), scans the configured `spiraldb_path` per spec-data-model L256–278, skips `QuestMetadatas/`, `GlobalRegistry/`, non-JSON and the `QuestTemplates/droptables/` subdirectory, extracts `m_questName` / `Name` / `TemplateID` (as a **string**) / `DeckName` / `ZoneName`, parses leniently (strict `JSON.parse` → JSON5, so legacy trailing commas are tolerated) and counts unparsable files instead of throwing, tolerates a missing directory (`NpcDropTable/` does not exist in the fork today), inserts everything with `status='extracted'` in one transaction, and dedupes duplicate keys **within** a type with a deterministic name-sorted "first file wins" while reporting them. Measured on the owner's fork: **2,271 imported, 0 unparsable, 3 skipped** — 322 quests / 317 drop_tables / 215 npc_inventory / 77 npc_spell_inventory / 134 creature_spellbook / 0 npc_drop_table / 1 treasure_card_inventory / 1,205 zone_transfer (the expected 1,207 minus 2 real duplicate `ZoneName` pairs: `WizardCity/Tutorial_Exterior` and `WizardCity/Tutorial_Interior` each appear in two files). The once-only client toast ("Imported {n} existing entries from SpiralDB") reads the **additive** `GET /api/status/_import` → `{ran,imported,imported_at}`, which reflects the current server process only (declared before `/:type` so it can never be shadowed, in-memory, no schema change and no new settings key).
- **D38 — User-identity gate (task 1.7 / story p1-09).** The `user_name` prompt is an **action gate, not a page-load popup**: spec-data-model L229 puts it on "first status transition or save", so `requireUserName()` resolves immediately with `settings.user_name` when it is non-blank and otherwise opens the dialog and resolves with the entered name **after** the save, letting the pending action resume by itself (measured: the triggering quest transition completed with no second click). Dismissing the dialog rejects with a cancellation error so the caller aborts without applying anything. Dialog rules: trimmed non-blank and ≤64 characters, `Save name` disabled while blank or whitespace-only, an inline error instead of a request on invalid input, `role="dialog"` + `aria-modal` + `aria-labelledby`, focus into the input on open, Escape cancels, Enter submits; it persists through `PUT /api/settings` and never re-asks (state lives in SQLite, so it survives reloads, browsers and sessions — the "not asked yet" state is exactly `user_name: ""`, per D32). The client data layer is `client/src/lib/api.ts` (`apiFetch` throwing an `ApiError` that carries the HTTP status and the server's `{error}` message, plus `getSettings`/`putSettings`/`getStatus`/`patchStatus`), which task 1.8 builds the shell on. Attribution stays correct even without the gate because the server still defaults `changed_by` to `settings.user_name` (D37) — both paths are evidenced. `client/src/App.tsx` currently carries a clearly-labelled **temporary diagnostic surface** (quest picker + "Mark reviewed") whose only purpose is to exercise the gate end to end before the shell exists; task 1.8 replaces that file wholesale and must keep `useUserNameGate`/`UserNameDialog`/`api.ts` as the durable pieces. **Two measured environment facts for all future UI work:** Vite 5 binds `[::1]:5173` only, so UI automation must use `http://localhost:5173` (127.0.0.1 refuses the connection), and playwright-mcp can only write inside `~/.cache/playwright-mcp`, so screenshots must be copied into `docs/evidence/phase-{n}/` afterwards.
- **D39 — App shell, shared components and the vendored primitive policy (task 1.8 / story p1-10).** The shell is the measurable shape of spec-ui-design L47–109: 260px fixed `zinc-900` sidebar with a `zinc-800` right border, 56px sticky `zinc-900` header, `zinc-950` app background, four collapsible groups (OVERVIEW / QUESTS / DATA / SETTINGS) whose 12 items are exactly spec L73–93, active item `blue-600/10` background + `blue-400` text, hover `zinc-800`, Lucide 18px icons, page title derived from the route, and a header user area showing the avatar initial + the `settings.user_name` from D38. Every route in spec-api L325–350 exists; pages not yet built render an "Arrives in Phase N" stub, with the mapping Dashboard→5, quests→2 (detail/edit→3), the eight data types→4, `/settings` fully built here. **Primitive policy:** the story vendors shadcn/ui-style primitives by hand under `client/src/components/ui/` (a `cn()` helper in `client/src/lib/utils.ts`) instead of running the interactive `shadcn` CLI, because that CLI is interactive and network-dependent and this run is unattended; only primitives that real code imports are shipped (button, card, badge, input, dialog, popover, command, table, skeleton), and `tabs`/`accordion`/`checkbox`/`slider`/`dropdown-menu`/`select`/`progress` arrive with the Phase 3/4 forms that first need them — no unused vendored files. **FriendlyNameDropdown (D8, extended):** `useNames(type)` is TanStack Query with `staleTime: Infinity`; the six bulk types (`items`, `spells`, `npcs`, `quests`, `zones`, `drop_tables`) are cached whole and filtered client-side in a cmdk combobox **capped at 50 rendered options with an explicit "showing 50 of N matches — keep typing" hint**, measured at ~2.98 ms per keystroke filter over all 79,835 items, so no virtualization is warranted yet; **`strings` is never bulk-loaded** (216,991 rows ≈ 24 MB) and instead uses the D36 server-side `?q=`/`?limit=` extensions above a minimum query length. The component keeps the raw id in a real hidden input (AGENTS.md rule 5) and displays per D8: plain names for items/spells/drop_tables, uniformly `"Name (TemplateID)"` for NPCs (`ArrotToMerles (161001)`), the server's already-humanized zone `display_name` (`Aquila/AQ_Z00_Hub` → "Aquila / AQ Z00 Hub"), and the raw key/id as-is on a lookup miss. **Toast policy:** sonner bottom-right, `visibleToasts: 3`, success 5 s / error 10 s / info 5 s, and a per-type left-border accent (`border-l-4` + `border-l-emerald-500` / `border-l-red-500` / `border-l-blue-500`) applied through `toastOptions.classNames`. *Measured trap for future styling of vendored internals:* `* { @apply border-border }` plus preflight's `border-width: 0` both score `(0,0,0)` and tie with sonner's `:where([data-sonner-toast])` rules, which Vite injects **before** `index.css` — so sonner's own borders silently lose and every toast renders accentless; Tailwind utilities are `(0,1,0)` and win regardless of order, and `classNames.default` must never carry a colour because sonner merges it into every type. *Recorded deviation:* the spec's "click to dismiss" for error toasts is implemented as sonner's real × close button (`aria-label="Close toast"`), because sonner has no click-anywhere dismiss. `notifyInfo` still has no caller — info toasts arrive with Phase 2's "Extracting quests… this may take a moment"; the blue accent is wired, unit-tested and present in the built CSS. The mockup's `[Browse]` buttons are omitted: a browser cannot pick a server-side directory path. Primitives and the `[Browse]` omission are scope reductions, not failures — the acceptance criteria of task 1.8 are all met.
- **D40 — Tier-1 UI harness and the hermetic-spec rule (task 1.10 / story p1-13).** `@playwright/test` is pinned **exactly** (`1.63.0`, chromium revision 1243) and drives `tests/ui/**` through `playwright.config.ts` (chromium headless only, `baseURL http://localhost:5173` per D38, `retries: CI ? 1 : 0`, `list` + html reporters into the gitignored `playwright-report/`, `webServer` = `SPIRALDB_UI_SKIP_IMPORT=1 npm run dev` with `reuseExistingServer: !CI` and a 120 s timeout against a measured 0.9 s boot). **`PLAYWRIGHT_BROWSERS_PATH=$PWD/tools/.playwright-browsers` lives in the npm scripts** (`test:ui`, `test:ui:install`) so the install and the run cannot disagree, and `ci.yml`'s install step calls `npm run test:ui:install` — a bare `npx playwright install chromium` writes to the default `~/.cache/ms-playwright` while the config reads the workspace path, a mismatch that would have failed the Phase-1 PR's own check run (measured, fixed before the gate rather than at it). **Tier-1 specs must be hermetic:** CI has no sibling repos, no WAD data and no pre-existing database, so the webServer skips the first-startup import and assertions that depend on ambient data use `page.route()` mocking (`/api/sync`, `/api/settings`); the real 22 s sync stays covered by tier-2 evidence (D23) and the sync unit tests, and a spec-header comment says so. **A tier-1 spec must never pin a known defect as expected** — the harness immediately found that `/quests/extract` highlighted two sidebar items, because the Sidebar took its highlight from `NavLink`'s prefix `isActive` while the already-correct `activeNavPath` helper went unwired (adding `end` everywhere would have broken legitimate detail-route highlighting, e.g. `/quests/DS-ACAD-C01-001` → Browse Quests); the highlight and `aria-current` now come from `activeNavPath`, and the mutation that pinned "expected 2 active" was deleted in favour of an exactly-one-active assertion on every route. **`tests/**` and `playwright.config.ts` are now typechecked** (`tests/tsconfig.json` + `npm run typecheck:tests`) — `tests/unit/**` had been covered by no tsconfig at all, and the coverage instantly found 4 genuine type errors. Measured trap for anyone writing a one-off typecheck: an ad-hoc tsc invocation *missing* the `@shared`/`@server` path aliases reports 106 errors of which the 4 real ones are invisible, because unresolvable imports degrade to `any` — missing aliases are a noisy false-*negative*, not the false-positive one would assume. Host note: NixOS cannot launch the downloaded `chromium_headless_shell` (`libglib-2.0.so.0`), so the suite runs via a patch inside the gitignored `tools/.playwright-browsers/` tree that a re-install would drop (CI's ubuntu-latest is unaffected); README documents it. Standard gate list for every later story is therefore: `npm test`, `npm run test:ui`, `npm run lint`, `npm run typecheck:tests`, both `tsc -p … --noEmit`, `npm run build`.
- **D41 — Certifying an AC whose evidence only a later gate can produce (story p1-12).** p1-12's criterion names *the Phase-1 PR's own check run* as its evidence, and only the `gate-1` story opens that PR. So the story's **deliverable** was completed and **pre-verified** by simulating a runner with a fresh local clone — `git clone --local` into a path named `spiraldb-ui` (the layout `actions/checkout` produces), then the workflow's exact step sequence with raw per-step output: `npm ci` (492 packages, lockfile unchanged) → `npm test` (510) → `npm run build` → `npm run lint` → `npm run typecheck:tests` → `npm run test:ui:install` (chromium revision 1243 into `$CLONE/tools/.playwright-browsers`, proving the npm-script browser path is the single source of truth even though `~/.cache/ms-playwright` exists on this host) → `npm run test:ui` (8 passed). The preflight proves the clone had **no** `node_modules`, `data/` or `tools/`, and the run proves the server creates `data/spiraldb-ui.db` itself via the `mkdirSync` at `server/src/db.ts` L121. Its **ledger certification stays OPEN** until the PR's `ci` check run is green, at which point `gate-1` records that run as p1-12's evidence **before** merging; the next-story pointer therefore deliberately skips p1-12 to p1-14 rather than re-doing it. **No story is ever certified on a proxy** — a local simulation may pre-verify a deliverable, but the AC's named artifact is what certifies it. Two measured consequences are recorded with it. (1) A runner-faithful proxy must name the checkout directory after the repo, because `actions/checkout` does: running the same sequence in `/tmp/ci-sim-<ts>` produced a *false* red, and that difference is exactly what exposed a real defect. (2) That defect was `tests/unit/db.test.ts` asserting `path.basename(resolveRepoRoot()) === 'spiraldb-ui'` while `resolveRepoRoot` matches on **manifest content**; it passed here only because this checkout happens to carry that name, and it would have failed `npm test` for **every** story run from the c5 worktree layout `.omd/worktrees/{run-id}`. Fixed in `5498fe1` (content + structural assertions, plus start-directory independence and the terminating error path), proven by running the suite in a differently-named clone (1 failed before → 20 passed after) and in the `.omd/worktrees/run-abc12345` shape (20 passed). Also recorded for the gate: a job-level `name:` would silently change the required check-run context away from the job key `ci` (every gate merge would then 405 forever), so `ci.yml` carries an explicit do-not-add comment and keeps the job key bare; and `runs-on` is pinned to `ubuntu-24.04` because `playwright install` resolves the browser build per OS release.
- **D42 — `git_branch` is API-only in the Phase 1 settings UI (measured during the p1-14 tier-2 pass).** The settings page edits **four** values — `aurorium_path`, `imcodec_path`, `spiraldb_path`, `user_name` — while `GET /api/settings` returns **five** keys (D32). Measured live: the four rendered inputs equal the API's values exactly, and `git_branch` renders nowhere in the client (`grep` finds it only in `client/src/lib/api.ts`'s `SettingKey` union). This is deliberate for Phase 1 and consistent with the spec's own mockup (`docs/spec-ui-design.md` L490–504 draws only the path fields; `spiraldb_path`/`user_name` are legitimate additions from D17/D32/D38), but it is a **known forward gap**: the Phase 2 save path auto-commits to `git_branch`, so a user who cannot edit it is pinned to the seeded `content/2026-09-25` until a new content drop. **A later phase (2 or 3) must surface `git_branch` as an editable settings field when the commit UI lands** — recorded here so the omission is a scheduled gap, not a silent one. `PUT /api/settings` already accepts it (D32), so the UI change is the only work.
- **D43 — the user-identity dialog has no reachable caller in the Phase-1 UI (found by the gate-1 boundary re-run; the dialog's first real caller is Phase 2 work).** Measured at the phase boundary: `UserNameGateProvider` is mounted in `App.tsx`, and `UserNameDialog`, `useUserNameGate` and the pure decisions in `lib/user-name.ts` are all present and durable — but **no component calls the gate and no component calls `patchStatus`** (the only reference outside the hook/dialog pair is the hook's own doc comment). Task 1.8 replaced the task-1.1 `App.tsx` diagnostic surface (quest picker + "Mark reviewed") exactly as D38 anticipated, and Phase 1 ships every editor route as a stub (D39 item 7), so in Phase 1 there is no UI entry point that performs a status transition or a save. Consequence: the "user-identity modal appeared first when `user_name` was empty" clause of the Phase-1 acceptance list (L95) is **not reproducible in the Phase-1 UI** and the dialog's rendering is exercised by no test. What *is* verified: the decision logic by `tests/unit/user-name-gate.test.ts`; the server attribution path by a PATCH with `user_name: ""` returning 200 with `changed_by: ""` / `reviewed_by: ""` (D38's documented default); and the reachable identity path end to end through the Settings page — `PUT /api/settings`, survival across a reload, and the following transition recording `reviewed_by: "Jason"`. **Phase 2 must therefore (a) make the first editor/save action the first real caller of `requireUserName()` and (b) add a tier-1 Playwright spec driving that action — asserting the dialog appears when `user_name` is empty, that the name persists through `PUT /api/settings`, and that the pending action resumes without a second click (the D38 semantics).** Until then this clause stays deferred with this home; it must not be reported as satisfied.









- **D48 — the extraction output's enum representation, and the save pipeline's measured corpus contract (added during p2-05; amends D45(1)).** **(a) The CLI's output settings gain `StringEnumConverter`** (`tools/PacketReaderCli/Program.cs`). The corpus writes every enum-valued field as its **name** — measured: `m_goalType` ×772, `m_operator` ×673, `m_activityType` ×322, `m_bountyType` ×5, plus `m_teleportType`/`m_routingType` ×1 — whereas D45's settings (Imlight's own: `TypeNameHandling.Auto` + `NullValueHandling.Ignore`) serialise enums as integers. Unfixed, **every** quest our pipeline saved would have rewritten those fields (`m_activityType` sits on all 322 quests), i.e. diff noise in the owner's corpus and a permanent representation split between our files and the corpus. Imlight has no converter but Newtonsoft deserialises a name *or* a number into an enum, so names stay loadable; p2-03's `verify:captures` normalises both (`typeof t === 'number' ? GOAL_TYPES[t] : String(t)`) and stayed green after the change (5 fixtures, 50 field checks, 0 mismatches), and the real CLI's body vs `questtemplates_WC-UNICORN-MAIN-004.json` now shows **zero value differences on every shared key**. **(b) Format normalisation is spec-sanctioned, one-time and unavoidable**: measured across the 322 corpus quests, 306 carry a trailing comma before a closer, 306 have no final newline, 317 contain integral floats (`0.0`), and files carry explicit `null`s that `NullValueHandling.Ignore` drops. spec-data-model L247-253 explicitly mandates clean JSON, so the first *update* of a legacy file normalises it (floats respelled, commas removed, newline added) with no value change; the p2-05 merge keeps keys the incoming payload omits, so the corpus's explicit nulls survive (proved 19/19 on a legacy file). p2-06/p2-08 should expect and can surface this normalisation instead of treating it as a defect. **(c) `entry_status` has no `extracted_by` column** (`server/migrations/0001_init.sql` L17-36: `extracted_at` plus `reviewed_by`/`verified_by` only) although the plan/spec wording implies one, so the save pipeline writes `extracted_at` on the initial insert and records the actor on the `status_history` row's `changed_by` — the history row *is* the extraction actor record. No migration was added. **(d) Metadata pairing is genuinely ambiguous for 8 quest names**: `QuestMetadatas/` holds 324 files covering 316 distinct `Name`s, so 8 names (`WC-UNICORN-MAIN-003/004/006/008`, …) have **two** metadata files each. D20's "scan for the file whose `Name` matches" therefore has no unique answer; p2-05 picks deterministically (first in name order) and p2-06 must surface the ambiguity (refuse, or disambiguate by `QuestTemplateId`) rather than silently choosing. **(e) Sibling git hazard, measured:** `simple-git` 3.36's `blockUnsafeOperationsPlugin` rejects a `commit` whose child environment carries `EDITOR`/`VISUAL`/`GIT_EDITOR`/`PAGER` ("Use of \"EDITOR\" is not permitted without enabling allowUnsafeEditor") and this host sets `EDITOR=nano`, so the git service builds an explicit allow-list environment instead of forwarding `process.env` — never disable that safety plugin to work around it. **(f)** Corpus directory reality the index must tolerate (D19/D35): `NpcDropTable/` does not exist in the corpus at all (an empty directory, not an error) and `ZoneTransfer/` contains genuine duplicate keys (`WizardCity/Tutorial_Exterior` and `…_Interior`).
- **D47 — D9's abort signal is the *response's* `close`, and the extraction endpoint's spec-silent choices (added during p2-04).** **(a) `req.on('close')` as written in D9/§2.2 is a no-op — it must be `res.on('close')`.** Measured on Node 24: `IncomingMessage` emits `close` when the request *body* ends, and multer has already consumed the body by the time the handler runs, so a listener registered there never fires and a real client abort would leave the CLI running to completion (the tests caught this on the first implementation). `res.on('close')` fires on completion *and* on a prematurely terminated connection, so the route treats it as an abort only while the response is unfinished (`res.writableEnded || res.finished` — measured: on a normal response the event arrives with `writableEnded === true`). Verified both ways: a 5.2 s extraction aborted with `curl --max-time 2` has its child present at t=0.08 s and **gone at t=2.02 s** (~20 ms after the abort), with `ps -C imview-packet-reader` empty afterwards and no stray children. **(b) Status codes** (the spec shows only the body): 400 for a capture the CLI could not parse, an unsupported extension, or a missing `file` field; 413 over the 512 MB cap; 500 when the CLI is not built. Always the `{error}` envelope, with `.status` set so the shared middleware agrees. **(c) Uploads land in `<repo root>/data/uploads/`** (inside the gitignored `data/`) and the temp capture is deleted when the request settles, success or failure; mid-transfer aborts are multer's own cleanup. The directory is created at **upload** time, never at import: multer's `diskStorage` `mkdirp`s a *string* destination inside its constructor, so a string destination would make merely importing `app.ts` touch disk (hence the callback destination — the router mounts directly, with no import-time side effects). **(d) No default execution timeout** — the spec defines extraction as a blocking call and the cancel path is the client abort; `timeoutMs` stays injectable. **(e) `DOTNET_ROOT`** (D45(3)'s home, now implemented): an explicit non-blank env value wins, otherwise the first `dotnet` on `PATH` is realpath'd and its directory taken, otherwise **no** variable is set and the apphost's own stderr is surfaced. It is a pure, injectable function, and the end-to-end proof ran with `DOTNET_ROOT` unset in the server's own environment. **(f) The 50 MB stdout cap is reachable with a *legal* capture, so the `--output` escape hatch is a real recovery path, not a corner case** — and it was exercised for real, not only unit-tested: a 3200-quest capture (105 MB upload) whose CLI stdout is ~55 MB indented overflowed the cap, the service retried with `--output`, and the API still answered `200 {count: 3200}` (the warning `[extract] CLI stdout exceeded the 50 MB buffer …; retrying with --output file mode` on stderr, no temp dirs or uploads left behind). Note the CLI emits `Formatting.Indented` (the corpus format, D45) while the response is compact — 38.5 MB vs 55 MB for the same payload — and the spec fixes no formatting. Node 24 reports the overflow as **`ERR_CHILD_PROCESS_STDIO_MAXBUFFER`** (the plan's `ENOBUFSRANGE` is the pre-Node-12 name); the service accepts every spelling. **(g) Flagged:** a parse-failure detail embeds the server-side temp upload path (`…/data/uploads/<uuid>.json`). The file is already deleted by the time the client reads the message and this is a local tool, but a later story may want it scrubbed.
- **D46 — task 2.1a's blob layouts, the four upstream reader defects, and the wrapper repairs (added during p2-03; the story's own AC list is otherwise unamended).** The fixtures are manufactured by re-serializing corpus objects with the **same** Imcodec serializer the reader uses, so no byte layout is hand-written; what follows is everything measurement contradicted. **(1) The plan's "offset 1/16" are `PropertyFlags` masks, not byte offsets** — `ObjectSerializer.Deserialize<T>` has a `uint propertyMask` overload (`ObjectSerializer.cs:216`) and `Prop_Save = 1`, `Prop_AuthorityTransmit = 16`; there is no padding byte, and the write side is the same call (`Imview/src/Imview.Core/Services/TemplateSerializerService.cs:130-134` is the canonical precedent). GoalCompilation/ClientTagList/MadlibBlock read with mask 1, ActorDialog with mask 16. **(2) `MSG_QUESTOFFER.Level` is unrecoverable through the reader**: the field has no `ExtractMethod`, so `PacketReaderService.GetDefaultValue` does `node["value"].GetValue<object>()` (a `System.Text.Json.JsonElement`) and `Convert.ChangeType` throws, swallowed into `default(int)` — reproduced with the level as number, string and float, and it would have written **level 0 into the owner's corpus for 286/322 quests**, not merely failed a comparison. Because the capture's level is ours to read and Node must never parse captures (architecture rule 4), the **CLI wrapper restores it from the same capture it already validates** (`ApplyOfferLevels`), which is a deliberate, documented repair rather than a faithful-passthrough wrapper. **(3) The reader's own logging can corrupt the payload**: a non-empty dialog `Persona` makes `TemplateManifestService` `Console.WriteLine` a "Could not find template ID" warning, so the wrapper keeps `Console.Out` redirected to stderr for the duration of the build call — proven on the failing path (107 KB of valid JSON on stdout, the warnings on stderr). **(4) `AddCompletionDialogToQuestTemplate` ignores `GoalID`** (it matches `CompletionType == "Completion"` and the quest id), so the fixtures write `QuestID 0` on goal-scoped dialog packets; a real capture will show one bogus quest-level dialog in the review UI — a product consequence for the extraction/review stories. Also recorded: `MSG_QUESTOFFER.Rewards` is never read by the reader; `BitWriter.GetData()` silently drops a pending partial byte (the generator zero-pads and self-checks both directions); and the corpus stores enums as **names** (`"GOAL_TYPE_WAYPOINT"`, `"ACTIVITY_NotActivity"`), so any JSON reader of the corpus needs `StringEnumConverter` (D45's reader settings). **Honest scope of the milestones:** 181 of 322 corpus quests are generatable; the 141 refusals are loud and each names its reason (130 goal-dialogs attached to compilation goals, 26 + 26 ACHIEVERANK, 11 with no goals, 5 with malformed tags, 3 duplicate ids). **ACHIEVERANK cannot round-trip at all** — its 4 quests carry empty `m_goalTitle` and `m_goalNameID 0`, breaking the reader's `{n}_{title}` naming invariant — so the "rarest corpus goal types" fixture uses the rarest **generatable** exact enum values instead (`GOAL_TYPE_USAGE` ×4 and `GOAL_TYPE_BOUNTY`, 4 quests each), covering 6 of the 7 supported goal types; that substitution must be flagged in the Phase-2 PR rather than quietly passed off. Measured round-trip after the repairs: 5 fixtures, **50/50 field checks, 0 mismatches**, including level, goal names in order, goal types and dialog block/entry counts, with the fixtures byte-reproducible from the generator (sha256-pinned in `server/test/fixtures/captures/README.md`).
- **D45 — the CLI wrapper's JSON contract, its capture-shape gate, and the .NET apphost environment (added during p2-02).** Three measured refinements to task 2.1, all recorded because a later task inherits them. **(1) Output format.** The spec's `csproj` snippet (L560–575) shows no package reference because it also omits serialization detail; the SpiralDB corpus and review files are **Newtonsoft** JSON whose polymorphic members carry `$type` (`spec-domain-reference.md` L314/L412, notes L714–717), and the game server's own settings are `TypeNameHandling = TypeNameHandling.Auto, NullValueHandling = NullValueHandling.Ignore` (`Imlight/src/Imlight.CoreLib/WizardData/SpiralDB.cs` L48–51). The wrapper therefore adds `Newtonsoft.Json` **13.0.3** — the version every Imcodec/Imlight project pins, and none of it is transitive from `Imview.PacketReader` — and serializes with exactly those two settings plus `Formatting.Indented`. `TypeNameHandling.Auto` reproduces the corpus shape faithfully: the root object carries **no** `$type` while each `m_goals` element carries `"Imcodec.ObjectProperty.TypeCache.{Type}, Imcodec.ObjectProperty"`. Measured consequence, flagged rather than hidden: the committed corpus files carry **explicit nulls** (`"m_questInfo": null`) while these settings omit them, so writing extracted quests back drops null fields — semantically identical for Imlight's name-keyed loader, but a diff-noise source the save story (**p2-05**, task 2.4/2.5) owns keeping minimal. **(2) Capture-shape gate.** `QuestBuilder` silently returns an empty list for anything it cannot match, so a non-capture would look like success and p2-02-ac2's "non-capture input → exit 1" could not hold. The wrapper therefore validates before calling the builder: the file must exist, parse as JSON, and look like a capture — a top-level array (or single object) whose entries are objects carrying a string `data.name` and an object `data.fields`. A capture that parses but contains no quest packets is still a legitimate **empty result** (exit 0 with `[]`, stated in `--help`). Measured matrix: not-JSON → 1, `{"foo":1}` → 1, `[{"data":{"name":"MSG_X"}}]` (no `fields`) → 1, nonexistent input → 1, missing `--input`/unknown flag → 1 with usage on stderr, `[]` → 0 with stdout exactly `[]`, `--output <file>` (creating parent directories) writes the file with empty stdout, `--output -` → stdout. Everything except the payload goes to stderr so the Node caller can `JSON.parse(stdout)`. `tools/bin/imview-packet-reader` is a **relative** symlink into `tools/.artifacts` and the release directory carries the apphost, `deps.json`, `runtimeconfig.json`, `Imview.PacketReader.dll`, the Imcodec DLLs and `Newtonsoft.Json.dll`, so the symlink is a complete program on a stock install. **(3) The apphost needs `DOTNET_ROOT` on NixOS — the one thing that does not work bare.** `tools/bin/imview-packet-reader --help` exits **131** with "You must install .NET to run this application… Failed to resolve libhostfxr.so", because NixOS keeps dotnet at `/run/current-system/sw/share/dotnet` with no `/etc/dotnet/install_location`, and `dotnet` on `PATH` is a wrapper script — which is why `dotnet build` works while the apphost cannot find its runtime. Supplying `DOTNET_ROOT` makes every probe above pass (measured). The build and the symlink are correct as specified; only *running* needs the variable. **Adopted resolution: the caller passes it portably** — `DOTNET_ROOT=$(dirname "$(readlink -f "$(command -v dotnet)")")`, which resolves to the SDK's `share/dotnet` on this host and to the right root on a stock install, so **p2-04** (the extraction service, spec L606's `execFile` with no `env`) must pass it in the child environment and cover it with a test; a stock CI runner (`ubuntu-24.04`) needs nothing. A host-wide alternative the owner may apply as a one-liner is a root-owned `/etc/dotnet/install_location` containing `/run/current-system/sw/share/dotnet`, which is recorded as a **suggestion, not a requirement** (no agent-side install, per the run's owner-prerequisite rule). Also noted: `ci.yml` never runs the CLI today, so if a later story's test invokes the real binary, CI must add `actions/setup-dotnet` first — home **p2-04** / the **gate-2** workflow decision.
- **D44 — the tier-1 UI harness runs against its own throwaway database, seeded for tests (added during p2-01's boundary check).** `playwright.config.ts` booted the dev stack with no database isolation (`reuseExistingServer: !process.env.CI`) against `data/spiraldb-ui.db`, whose `spiraldb_path` is the owner's real fork. That was harmless while Phase 1 only read, but the first spec that drives a Phase-2 extraction or save would have written into the owner's repository — which D17 forbids. The harness now boots `rm -f data/test-ui.db && NODE_ENV=test SPIRALDB_UI_DB=$PWD/data/test-ui.db SPIRALDB_UI_SKIP_IMPORT=1 npm run dev` with `reuseExistingServer: false` (a developer's own server holds the live database, so reusing it would silently drop the isolation it exists to give). Two pieces make it work: `resolveDbFile(env, repoRoot)` returns `SPIRALDB_UI_DB` when set and non-empty, else `defaultDbFile()` — honoured only by the app's own connection `getDb()`, so `openDb()` callers, scripts and unit tests keep passing a path explicitly — and first-run seeding (D31c) with `NODE_ENV=test` yields `data/test-spiraldb` (D17). The boot line now reports `resolveDbFile()`; it previously logged `defaultDbFile()` and so named the developer's database while the server had opened a different one. p2-01's "dev/test settings point `spiraldb_path` at it" is read as *automated-test configuration*: the human dev/production server keeps the owner fork, since D17 reserves the real fork for owner-driven production use and D31c never rewrites an existing database from an env var. Verified: 8 UI specs green with the boot line naming `data/test-ui.db`; the live database byte-identical (sha256 prefix `03e37e43ca9f8692`, mtime unchanged); the isolated database seeded `spiraldb_path=<repo>/data/test-spiraldb`, 12 tables, 0 `entry_status` rows; plus 2 new tests in `tests/unit/db.test.ts` (514 total).

## Phase log (merged milestones)

Each phase ends with a gate story, its own PR and the required `ci` check run; the
entry here records the merge so the durable doc carries it (the full run log lives
in the gitignored `.omd/prd/progress.txt`).

| Phase | Branch / PR | Required `ci` check run | Merged | Gate story | Notes |
|---|---|---|---|---|---|
| 1 — Foundation | `phase-1-foundation`, [PR #3](https://github.com/jasonl8446/spiraldb-ui/pull/3) | [36224267657](https://github.com/jasonl8446/spiraldb-ui/actions/runs/36224267657) (job 108355143405, 39 s, all 14 steps) | `23d4b8a` | gate-1 | 19/19 Phase-1 checkboxes re-run fresh from `rm -rf data`; evidence in [`docs/evidence/phase-1/`](./evidence/phase-1/). Two gaps carried forward with homes: **D42** (API-only `git_branch`) and **D43** (identity dialog has no Phase-1 caller). Branch deleted after merge; `main` carries the full branch history (the merge commit has two parents). |

## Spec gaps & open questions — ALL RESOLVED (2026-09-25)

- **Q1 — GlobalRegistry verification tracking → RESOLVED: no tracking.** 8 types carry the extracted→reviewed→verified lifecycle; GlobalRegistry gets an editor only. Follows the detailed specs ([spec-data-model.md](./spec-data-model.md) L32–37, L277; [spec-api.md](./spec-api.md) L56) over AGENTS.md's "all 9 types" wording. Owner-confirmed. AGENTS.md wording to be corrected in task 5.7.
- **Q2 — .NET SDK provisioning → RESOLVED: owner pre-installs once** (prerequisite 1) + sandbox-safe build flags (D18). No mid-run approvals needed.
- **Q3 — Delete support → RESOLVED: none in v1** (D6 stands). Deletion remains a manual git operation. Owner-confirmed.
- **Q4 — Stale README links → RESOLVED 2026-09-25 (pre-Phase-1):** README rewritten to link the five specs + six plan docs, with a project-status note; AGENTS.md Q1 wording corrected in the same pass. Task 5.7 still re-audits both against implemented reality (prerequisites, scripts, `npm run build:cli` flags).
- **Q5 — GlobalRegistry multi-file semantics → RESOLVED: consolidate & replace (D22)**, based on verified Imlight loader behavior (`SpiralDB.cs` L341–347). Owner-confirmed.

## Quality bar (applies to every phase)

1. **Fidelity first.** D5 + corpus round-trip tests gate Phases 3–4: parse → serialize → re-parse must deep-equal for every existing SpiralDB file of each touched type.
2. **Validation per spec.** Quest rules ([spec-domain-reference.md](./spec-domain-reference.md) L528–534), DropTable rules (L536–540), general rules (L542–546): inline field errors, form-level banner, Save disabled while errors exist (L547). Server re-validates on every POST.
3. **Acceptance criteria are commands.** ≥90% must be checkable by running something (curl/npm/git/vitest/Playwright) and observing output — no "looks right".
4. **Spec deviations get recorded.** Any divergence from `docs/spec-*.md` lands in this overview's decisions list in the same PR.
5. **External repos are read-only** except: the test clone `data/test-spiraldb/` (written freely by tests), and the owner's fork in production use only (D17). Imview/Aurorium/Imlight/Imcodec trees are never written (D18).
6. **Unattended-evidence rule (D23).** Every UI criterion ships Playwright assertions + screenshots; every data criterion ships command output. Evidence paths are linked in the phase PR.
7. **Regression gate (D16→D29).** No phase starts before all previous phases' acceptance suites re-pass — enforced as PRD gate stories under the same evidence contract as every other story.

## Milestone exit criteria (summary)

| Milestone | Exit criterion (one line) |
|---|---|
| M1 (Phase 1) | `npm run sync` populates friendly names from the real Root.wad; names/status/dashboard/settings APIs live against real data (322 quests imported); app shell navigates all routes |
| M2 (Phase 2) | A packet capture uploaded through the UI becomes committed quest files + metadata on a `content/YYYY-MM-DD` branch with `extracted` status |
| M3 (Phase 3) | Any of the 322 real quests can be fully edited (goals, logic, requirements, results, dialog) with validation and zero field loss (round-trip proven) |
| M4 (Phase 4) | All 8 remaining object types + GlobalRegistry have working list/detail/create/edit/save+commit flows |
| M5 (Phase 5) | Dashboard, global search, error/a11y/responsive passes done; `npm run build && npm start` serves the production app on :3001 |

## Related Documentation

- [spec-architecture.md](./spec-architecture.md) — system overview, tech stack, server config
- [spec-data-model.md](./spec-data-model.md) — SQLite schemas, lifecycle, file naming, git strategy
- [spec-api.md](./spec-api.md) — REST endpoints and frontend routes
- [spec-domain-reference.md](./spec-domain-reference.md) — JSON schemas, enumerations, validation, CLI wrapper, sync internals
- [spec-ui-design.md](./spec-ui-design.md) — layout, components, interaction patterns
